{
  "name": "tradelineiq-backend",
  "version": "1.0.0",
  "description": "Ready-to-deploy auth + application backend for the TradelineIQ front-end. Plugs straight into the /api/* hooks already in index.html.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "init-db": "node db.js --init"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.14"
  }
}